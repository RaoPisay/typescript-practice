/**
 * A demo of arrow function other wise known as Lamda expressions.
 * Samething what we did in ts1.ts doing with arryow functions.
 */
var log = function (message) { return console.log(message); };
log("Hello World!!");
